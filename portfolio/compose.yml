services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio:latest
    ports:
      - "8881:80"
    environment:
      - PRUEBA=Hola mundo desde Docker Compose
    networks:
      - portfolio-website
networks:
  portfolio-website:
    driver: bridge
